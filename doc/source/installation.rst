============
Installation
============

For CentOS, install dependency::

    $ yum install redhat-lsb

At the command line::

    $ pip install bindep

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv bindep
    $ pip install bindep
